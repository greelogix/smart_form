/// Defines the visual language for SmartForm fields.
enum SmartStyle {
  /// Always use Google's Material Design.
  material,
  
  /// Always use Apple's Cupertino Design.
  cupertino,
  
  /// Automatically switch based on the host platform (iOS/macOS vs others).
  adaptive,
}
